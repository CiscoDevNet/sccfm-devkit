**/vault.yml
**/.vault_pass~
examples/group_vars/all/vault.yml

# Token setup outputs (secrets — never commit)
**/.env

# LICENSE is copied from the repo root at build time (see cisco_sccfm_scripts/build_ansible_collection.py)
LICENSE
