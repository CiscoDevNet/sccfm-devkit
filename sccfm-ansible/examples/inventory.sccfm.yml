# Configure the selected profile first:
#   sccfm-cli --profile default configure --region us
plugin: cisco.sccfm.sccfm
profile: default
group_by_device_type: true
# limit: 100
# query: "deviceType:ASA"
# group: sccfm
